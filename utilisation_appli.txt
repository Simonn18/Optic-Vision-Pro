#installer pyinstaller 
pip3 install PySide6 PyInstaller

si fichier dist pas encore créer.
    pyinstaller --windowed main.py

pour lancer les mise à jour de l'appli:
        pyinstaller --windowed main.spec

application se trouve dans le dossier dist